# Server-side only. Never committed with a real value.
# Set in GitHub Actions secrets and (if using the narrative layer) Vercel env vars.
OPENROUTER_API_KEY="MY_OPENROUTER_API_KEY"

# Optional — powers the narrative explanation layer. The dashboard works without it.
GEMINI_API_KEY="MY_GEMINI_API_KEY"
